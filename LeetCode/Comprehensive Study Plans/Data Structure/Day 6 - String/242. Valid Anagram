class Solution:
    def isAnagram(self, s: str, t: str) -> bool:
        # Using the 'sorted()' function to both the strings & checking them if they are equal
        if sorted(s) == sorted(t) :
            return True 
        else :
            return False
        
